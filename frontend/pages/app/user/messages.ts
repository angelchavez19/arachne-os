import {
  configurationMessagesEn,
  configurationMessagesEs,
} from "./configuration/messages";

export const userMessagesEn = {
  configuration: configurationMessagesEn,
};

export const userMessagesEs = {
  configuration: configurationMessagesEs,
};
